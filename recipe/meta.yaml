{% set name = "coded-flows" %}
{% set version = "0.7.3" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.org/packages/source/{{ name[0] }}/{{ name }}/coded_flows-{{ version }}.tar.gz
  sha256: 0d531418e8ee1f98e84ee9ce4356d915bc20b5ae915005d9683f79b59a0613aa

build:
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
  number: 0

requirements:
  host:
    - python {{ python_min }}
    - poetry-core
    - pip
  run:
    - python >={{ python_min }},<4.0.0
    - pandas >=2.1.0,<3.0.0
    - pyarrow >=18.0.0,<19.0.0
    - pydantic >=2.6.4,<3.0.0
    - email-validator >=2.0.0,<3.0.0
    - pycountry >=24.6.1,<25.0.0
    - pillow >=11.3.0,<12.0.0
    - pydantic-extra-types >=2.6.0,<3.0.0
    - polars >=1.0.0,<2.0.0

test:
  imports:
    - coded_flows
  commands:
    - pip check
  requires:
    - pip
    - python {{ python_min }}

about:
  home: https://github.com/color-coded-codes/coded-flows
  summary: Various utilities for Coded Flows
  license: 'MIT'
  license_family: MIT
  license_file: LICENSE

extra:
  recipe-maintainers:
    - aminelemaizi
